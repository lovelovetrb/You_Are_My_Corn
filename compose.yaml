version: "3.9"

services:

  frontend:
    container_name: "you are my corn frontend"
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    volumes:
      - ./frontend/source:/usr/src/frontend
      - node_modules:/usr/src/frontend/node_modules
    restart: always
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"

  backend:
    container_name: "you are my corn backend"
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    volumes:
      - ./backend/app:/usr/src/backend/app
    ports:
      - 5000:5000

volumes:
    node_modules:
